extends ./layout/public-app


block contenido
    
    section(class="py-5")
        h2(class="text-center text-4xl font-extrabold pt-10")= title

        if properties.length
            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                each property in properties
                    include ./templates/card
        else
            p(class="text-center text-gray-800 text-sm py-10") No hay resultados

            //- pre= JSON.stringify(recentlyAddedHouses, null, 3)

        
